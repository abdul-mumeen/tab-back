version: '3.1'

services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 0987654321
      MYSQL_DATABASE: tabby
      MYSQL_USER: joshua
      MYSQL_PASSWORD: password
    volumes:
      - ./db-data:/var/lib/mysql
    ports:
      - 3308:3306
